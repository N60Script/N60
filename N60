<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>لتحميل </title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: black;
      color: white;
      text-align: center;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .progress-bar {
      width: 300px;
      height: 20px;
      background: #333;
      border-radius: 10px;
      overflow: hidden;
      margin-top: 15px;
    }
    .progress-fill {
      height: 100%;
      width: 0%;
      background: #4caf50;
      transition: width 0.2s;
    }
    #doneMessage {
      margin-top: 20px;
      font-size: 22px;
      display: none;
    }
  </style>
</head>
<body>
  <h1>كس بيرك يا وسيم</h1>
  <h2>كس بيرك يا مهند</h2>
  <p>جار فتحهم..</p>
  <div class="progress-bar"><div class="progress-fill" id="progress"></div></div>
  <p id="progressText">1%</p>
  <p id="doneMessage">❌ فشل فتحكم. لانكم مفتوحين</p>

  <script>
    let progress = document.getElementById("progress");
    let progressText = document.getElementById("progressText");
    let doneMessage = document.getElementById("doneMessage");

    let value = 1;
    let interval = setInterval(() => {
      value++;
      progress.style.width = value + "%";
      progressText.textContent = value + "%";

      if (value >= 100) {
        clearInterval(interval);
        doneMessage.style.display = "block";
      }
    }, 50); // سرعة التحميل (كل 50ms يزيد 1%)
  </script>
</body>
</html>
